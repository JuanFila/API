it("result of them sum of 2 + 2 must be 4", () => {
    const a = 2;
    const b = 2;
    const result = a + b;

    expect(result).toEqual(4) // expect = minha expectativa / to equal resultado
})
// it recebe 2 paramretros um deles é a descrição e depois a função
